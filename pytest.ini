[pytest]
norecursedirs = dist* build* artifacts* .*
testpaths = backend_py tests
markers =
    smoke: Gate 1 tests
    performance: Gate 2 tests
    quality: Quality Gate tests
    contract: Contract schema tests
filterwarnings =
    ignore::DeprecationWarning
    ignore::UserWarning
