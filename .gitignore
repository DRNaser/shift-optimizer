# =============================================================================
# SOLVEREIGN .gitignore
# =============================================================================

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
.pytest_cache/
.coverage
*.egg-info/
*.egg

# Virtual environments
.env
.venv
env/
venv/
ENV/

# Build artifacts
build/
dist/
dist_*/
*.local

# Node.js (if frontend is ever added back)
node_modules/
npm-debug.log*

# IDE
.vscode/*
!.vscode/extensions.json
.idea/
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.DS_Store

# Logs
logs/
*.log
debug_log.txt
server_log*.txt

# Temporary files
*.tmp
*.temp
NUL

# Data/Export artifacts (keep in .gitignore, generate fresh)
*.csv
*.xlsx
!backend_py/data/*.csv
artifacts/
exports/

# Backend temp files
backend_py/artifacts/
backend_py/*.json
!backend_py/pyproject.toml
backend_py/server.err

# Test artifacts
snapshot_iter_*.json
diag_run_result*.json
test_report.json
baseline_metrics_raw.txt
proof_*.txt
*_evidence.txt
evidence/

# Desktop builds (not used anymore)
ShiftOptimizer.spec
*.spec

# =============================================================================
# Guardian Context Tree - Volatile files (MUST NOT be committed)
# =============================================================================
# EXACT PATHS - verified working with `git check-ignore -v <path>`

# Generated by bootstrap, session-specific (volatile)
.claude/context/00-current-state.md

# Runtime state - ALL files (volatile, bootstrap-generated)
# CRITICAL: Never commit runtime state - may contain system info
.claude/state/runtime/**

# Telemetry files - ALL files (volatile, regenerated each session)
.claude/telemetry/**

# Archived skills (reference only, not active)
.claude/skills_archive/**

# =============================================================================
# SECURITY: Never commit secrets/tokens/credentials
# =============================================================================
# These patterns prevent accidental secret commits
**/secrets.json
**/credentials.json
**/*.pem
**/*.key
**/.env.local
**/.env.production

# Additional security patterns
**/.DS_Store
**/*.ndjson
**/*.log.json
**/error_dump*.json
data/osrm/
dist_final/
